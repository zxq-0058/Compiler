int dot_product(int c[10], int a[10], int b[10]) {
    int i = 0;
    while (i < 10) {
        c[i] = a[i] * b[i];
        i = i + 1;
    }
    return 0;
}



int main() {
    int a[10], b[10], c[10];
    int i = 0, j = 0, n = 9;

    // 对a、b数组进行赋值
    while (i <= n) {
        a[i] = b[i] = i;
        // write(a[i]);
        // b[i] = n - i;
        i = i + 1;
    }

    dot_product(c, a, b);  // 对a、b数组进行点乘

    // 输出c数组
    i = 0;
    while (i <= n) {
        write(c[i]);
        i = i + 1;
    }

    return 0;
}
// 输出结果是: 0, 1, 4, 9 ...
